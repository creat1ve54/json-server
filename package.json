{
  "name": "json-server",
  "version": "1.0.0",
  "description": "JSON Server for my app",
  "main": "index.js",
  "scripts": {
    "start": "json-server db.json --host 0.0.0.0 --port ${PORT:-3000}"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.6"
  }
}
